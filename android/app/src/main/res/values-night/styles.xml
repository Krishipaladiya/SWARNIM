<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied to the Android Window while the process is starting when the OS's Dark Mode setting is on -->
    <style name="LaunchTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <!-- Show a splash screen on the activity. Automatically removed when
             the Flutter engine draws its first frame -->
        <item name="android:windowBackground">@drawable/launch_background</item>

        <!-- Same navy in dark mode: the logo has one background colour. -->
        <item name="android:windowSplashScreenBackground">@color/swarnim_splash</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@color/swarnim_splash</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.

         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="NormalTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <!--
            The app's own navy, not ?android:colorBackground.

            On Theme.Black that placeholder resolves to pure black, and this is
            the window colour Android paints behind the Flutter surface - so it
            showed as a black flash between the launch screen and Flutter's
            first frame, and again behind any transparent edge at runtime.
        -->
        <item name="android:windowBackground">@color/swarnim_navy</item>
    </style>
</resources>
