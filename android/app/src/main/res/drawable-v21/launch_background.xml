<?xml version="1.0" encoding="utf-8"?>
<!--
    The window background Android paints before Flutter has an engine.

    Deliberately just the colour, with no logo on it. The launch animation opens
    on an empty field and draws the wordmark; putting the finished wordmark here
    first showed the ending before the beginning.
-->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/swarnim_splash" />
</layer-list>
